class StringConfig {
  StringConfig._(); //is a private constructor with the underscore prefix. The purpose of using `._()` is to prevent the class from being instantiated from outside of the class itself.
  static const String counterRoute = 'counter';
  static const String counterApp = 'Counter App';
}
